import { statisticsDatamodel } from './statistics-datamodel';

describe('statisticsDatamodel', () => {
  it('should work', () => {
    expect(statisticsDatamodel()).toEqual('statistics-datamodel');
  });
});
