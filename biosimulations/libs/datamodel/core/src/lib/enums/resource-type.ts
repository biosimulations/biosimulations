export enum ResourceType {
  project = 'project',
  model = 'model',
  simulation = 'simulations',
  chart = 'chart',
  visualization = 'visualization',
  simulator = 'simulator',
  file = 'file',
  user = 'user',
}
