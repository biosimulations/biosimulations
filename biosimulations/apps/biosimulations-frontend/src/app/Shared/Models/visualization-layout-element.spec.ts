import { VisualizationLayoutElement } from './visualization-layout-element';

describe('VisualizationLayoutElement', () => {
  it('should create an instance', () => {
    expect(new VisualizationLayoutElement()).toBeTruthy();
  });
});
