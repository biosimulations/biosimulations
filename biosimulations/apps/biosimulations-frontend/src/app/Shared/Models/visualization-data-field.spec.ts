import { VisualizationDataField } from './visualization-data-field';

describe('VisualizationDataField', () => {
  it('should create an instance', () => {
    expect(new VisualizationDataField()).toBeTruthy();
  });
});
