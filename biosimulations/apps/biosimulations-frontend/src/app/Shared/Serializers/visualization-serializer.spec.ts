import { VisualizationSerializer } from './visualization-serializer';

describe('VisualizationSerializer', () => {
  it('should create an instance', () => {
    expect(new VisualizationSerializer()).toBeTruthy();
  });
});
