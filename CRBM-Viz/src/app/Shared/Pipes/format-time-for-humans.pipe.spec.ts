import { FormatTimeForHumansPipe } from './format-time-for-humans.pipe';

describe('FormatTimeForHumansPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTimeForHumansPipe();
    expect(pipe).toBeTruthy();
  });
});
