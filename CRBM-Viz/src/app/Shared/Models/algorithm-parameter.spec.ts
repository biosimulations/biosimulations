import { AlgorithmParameter } from './algorithm-parameter';

describe('AlgorithmParameter', () => {
  it('should create an instance', () => {
    expect(new AlgorithmParameter()).toBeTruthy();
  });
});
