import { ChartTypeSerializer } from './chart-type-serializer';

describe('ChartTypeSerializer', () => {
  it('should create an instance', () => {
    expect(new ChartTypeSerializer()).toBeTruthy();
  });
});
